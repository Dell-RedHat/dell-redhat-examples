kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: sqlpad
  namespace: sqlserver
spec:
  to:
    kind: Service
    name: sqlpad-sqlserver-service
    weight: 100
  port:
    targetPort: http
  wildcardPolicy: None