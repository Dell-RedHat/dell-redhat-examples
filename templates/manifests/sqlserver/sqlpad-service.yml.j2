apiVersion: v1
kind: Service
metadata:
  name: sqlpad-sqlserver-service
  namespace: sqlserver
spec:
  selector:
    app: sqlpad-sqlserver
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 3000